<UserControl x:Class="DoAn_banPRO.Views.NhaCungCapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="QUẢN LÝ NHÀ CUNG CẤP" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Mã NCC:" Grid.Row="0" Grid.Column="0" Margin="5"/>
            <TextBox Text="{Binding SelectedNCC.MaNCC, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Margin="5"/>

            <TextBlock Text="Tên NCC:" Grid.Row="0" Grid.Column="2" Margin="5"/>
            <TextBox Text="{Binding SelectedNCC.TenNCC, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="3" Margin="10,5,-200,5"/>

            <TextBlock Text="Số điện thoại:" Grid.Row="1" Grid.Column="0" Margin="5"/>
            <TextBox Text="{Binding SelectedNCC.SoDienThoai, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Margin="5"/>

            <TextBlock Text="Địa chỉ:" Grid.Row="1" Grid.Column="2" Margin="5"/>
            <TextBox Text="{Binding SelectedNCC.DiaChi, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="3" Margin="5,5,-200,5"/>
        </Grid>
        
        <StackPanel Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Bottom" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Tạo mới" Command="{Binding AddCommand}" Width="70" Margin="0,0,5,0"/>
            <Button Content="Cập nhật" Command="{Binding UpdateCommand}" Width="70" Margin="0,0,5,0"/>
            <Button Content="Xóa" Command="{Binding DeleteCommand}" Width="70"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding NhaCungCapList}" SelectedItem="{Binding SelectedNCC}" 
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single" SelectionChanged="DataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã Nhà Cung Cấp" Binding="{Binding MaNCC}"/>
                <DataGridTextColumn Header="Tên Nhà Cung Cấp" Binding="{Binding TenNCC}" Width="200"/>
                <DataGridTextColumn Header="Số điện thoại" Binding="{Binding SoDienThoai}" Width="150"/>
                <DataGridTextColumn Header="Địa chỉ" Binding="{Binding DiaChi}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>