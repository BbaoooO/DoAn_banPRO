<UserControl x:Class="DoAn_banPRO.Views.PhieuXuatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="XUẤT KHO / BÁN HÀNG" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" Foreground="DarkRed"/>
        
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,0,5,0">
                <StackPanel>
                    <TextBlock Text="Thông tin phiếu xuất:" FontWeight="Bold" Margin="0,0,0,10"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="Mã phiếu:" Width="80"/>
                        <TextBox Text="{Binding CurrentPhieuXuat.MaPhieu}" Width="150" IsReadOnly="True" Background="#eee"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="Ngày lập:" Width="80"/>
                        <TextBox Text="{Binding CurrentPhieuXuat.NgayLap, StringFormat=dd/MM/yyyy}" Width="150" IsReadOnly="True" Background="#eee"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="5,0,0,0">
                <StackPanel>
                    <TextBlock Text="Thêm chi tiết xuất:" FontWeight="Bold" Margin="0,0,0,10"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="Linh Kiện:" Width="80"/>
                        <ComboBox ItemsSource="{Binding DanhSachLinhKien}" SelectedItem="{Binding SelectedLinhKien}" DisplayMemberPath="TenLK" Width="200"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="Số lượng:" Width="80"/>
                        <TextBox Text="{Binding SoLuongXuat, UpdateSourceTrigger=PropertyChanged}" Width="150"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="Đơn giá xuất:" Width="80"/>
                        <TextBox Text="{Binding DonGiaXuat, UpdateSourceTrigger=PropertyChanged}" Width="150"/>
                    </StackPanel>
                    
                    <Button Content="Thêm vào phiếu" Command="{Binding AddChiTietCommand}" Width="120" HorizontalAlignment="Right" Margin="0,10,0,0" Height="25"/>
                    <TextBlock Text="Không đủ tồn kho!" Foreground="Red" HorizontalAlignment="Right" Margin="0,5,0,0" Visibility="Collapsed"/>
                </StackPanel>
            </Border>
        </Grid>

        <DataGrid Grid.Row="2" ItemsSource="{Binding ChiTietPhieuXuats}" SelectedItem="{Binding SelectedChiTiet}" 
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã linh kiện" Binding="{Binding MaLK}"/>
                <DataGridTextColumn Header="Số lượng" Binding="{Binding SoLuong}"/>
                <DataGridTextColumn Header="Đơn giá xuất" Binding="{Binding DonGiaXuat, StringFormat={}{0:N0}}"/>
            </DataGrid.Columns>
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Xóa khỏi phiếu" Command="{Binding RemoveChiTietCommand}"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock Text="Tổng tiền:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="16"/>
            <TextBlock Text="{Binding TongTienPhieu, StringFormat={}{0:N0} VND}" FontWeight="Bold" VerticalAlignment="Center" Foreground="Red" FontSize="16" Margin="0,0,20,0"/>
            <Button Content="XUẤT KHO" Command="{Binding SavePhieuXuatCommand}" Width="130" Height="35" Background="DarkRed" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</UserControl>