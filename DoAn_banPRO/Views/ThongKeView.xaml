<UserControl x:Class="DoAn_banPRO.Views.ThongKeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="THỐNG KÊ &amp; BAO CAO" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
        <Button Content="XUẤT BÁO CÁO CSV (DESKTOP)" Command="{Binding XuatBaoCaoCommand}" Width="250" Height="30" HorizontalAlignment="Right" Background="#3b5998" Foreground="White"/>

        <!-- Tong hop -->
        <Grid Grid.Row="1" Margin="0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Border Background="#e6f2ff" Padding="15" Margin="5" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="TỔNG SỐ LINH KIỆN" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TongSoLinhKien}" FontSize="24" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#e6ffe6" Padding="15" Margin="5" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="TỔNG GIÁ TRỊ TỒN KHO" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TongGiaTriTonKho, StringFormat={}{0:N0}}" FontSize="24" HorizontalAlignment="Center" Margin="0,10,0,0" Foreground="Red"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="#fff0e6" Padding="15" Margin="5" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="TỔNG SỐ PHIẾU NHẬP" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TongPhieuNhap}" FontSize="24" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Danh sach chi tiet -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Ton kho thap -->
            <GroupBox Header="Linh kiện có tồn kho thấp (&lt;= 5)" Margin="5">
                <DataGrid ItemsSource="{Binding LinhKienTonKhoThap}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã linh kiện" Binding="{Binding MaLK}"/>
                        <DataGridTextColumn Header="Tên linh kiện" Binding="{Binding TenLK}" Width="*"/>
                        <DataGridTextColumn Header="Tồn kho" Binding="{Binding TonKho}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <!-- Phieu Nhap Gan Day -->
            <GroupBox Grid.Column="1" Header="Phiếu nhập gần đây" Margin="5">
                <DataGrid ItemsSource="{Binding PhieuNhapGanDay}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã phiếu" Binding="{Binding MaPhieu}"/>
                        <DataGridTextColumn Header="Ngày lập" Binding="{Binding NgayLap, StringFormat=dd/MM/yyyy}"/>
                        <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TongTien, StringFormat={}{0:N0}}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>